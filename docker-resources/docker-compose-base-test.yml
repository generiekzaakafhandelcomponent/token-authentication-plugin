services:
  db:
    image: postgres:15-alpine
    ports:
      - "54361:5432"
    environment:
      POSTGRES_DB: token-authentication-test
      POSTGRES_USER: test-user
      POSTGRES_PASSWORD: password
